# PyTorch (CPU/MPS build from PyPI). Works on macOS, Linux, and the Docker image.
# The web app runs sentence-transformers on CPU; the heavy LLM work is offloaded
# to the separate Ollama server, so no CUDA build is needed here.
# For a CUDA GPU host, install a matching wheel instead, e.g.:
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
torch==2.12.1

Flask==3.1.3
nltk==3.9.1
numpy==2.4.3
pandas==3.0.1
requests==2.32.5
scikit-learn==1.8.0
scipy==1.17.1
sentence-transformers==4.1.0

# Test suite
pytest==9.1.1
