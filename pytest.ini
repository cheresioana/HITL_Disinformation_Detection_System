[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    integration: end-to-end tests that load the narrative trees and call the live LLM judge (need tree fixtures + reachable Ollama; auto-skipped otherwise)
